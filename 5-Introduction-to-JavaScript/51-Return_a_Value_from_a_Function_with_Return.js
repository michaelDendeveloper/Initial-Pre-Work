/* I learned the use of arguments and return statements. Arguments are the values passed into the function, the return statement is used to send the result of the function operations out of the function. I created a function that multiplies vaule sent to it by five and returns the result.
*/
// Example
function minusSeven(num) {
  return num - 7;
}

// Only change code below this line
function timesFive(multiOne){
  return(multiOne*5);
}
console.log(timesFive(5));

console.log(minusSeven(10));
