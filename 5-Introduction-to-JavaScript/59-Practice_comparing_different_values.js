/* I learned that typeof operator returns the type of a variable, so it can be used to determine variable types.
*/
// Setup
function compareEquality(a, b) {
  if (a === b) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
compareEquality(10, "10");
